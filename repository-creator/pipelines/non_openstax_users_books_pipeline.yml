---
var_sources:
  - name: github
    type: dummy
    config:
      vars:
        username:
        token:
        email:
        organization:
        openstax:
          username:
          token:

docker-credentials: &docker-credentials
  username: ((ce-dockerhub-id))
  password: ((ce-dockerhub-token))

status-notifications: &status-notifications
  on_success:
    put: notify
    params:
      alert_type: success
  on_failure:
    put: notify
    params:
      alert_type: failed

resource_types:
  - name: queue-checker
    type: registry-image
    source:
      repository: golfpapagolf/queue-checker
  - name: slack-notifier
    type: registry-image
    # Switch back to mockersf/concourse-slack-notifier once
    # https://github.com/mockersf/concourse-slack-notifier/pull/86 is merged
    source:
      repository: aoldershaw/concourse-slack-notifier

resources:
  - name: queue
    type: queue-checker
    check_every: 1m
    source:
      uri: https://gist.githubusercontent.com/optimussam/4280485d3a3cb6aad21566f62d265e5a/raw/0338d6e142536ebc513943cd57ccc691686d579e/vendor_books.json

  - name: content-synchronizer
    type: git
    source:
      uri: https://github.com/openstax/content-synchronizer
      branch: sk-ce-1790

  - name: notify
    type: slack-notifier
    icon: slack
    source:
      url: ((slack-webhook-cestream))

jobs:
  - name: sync-a-book
    <<: *status-notifications
    public: true
    max_in_flight: 2
    plan:
      - in_parallel:
        - get: queue
          trigger: true
          version: every
        - get: content-synchronizer
      - task: sync-content
        config:
          platform: linux
          image_resource:
            type: docker-image
            source:
              repository: samuelklutse/openstax-cnx-migration
              tag: latest
              <<: *docker-credentials

          inputs:
            - name: queue
            - name: content-synchronizer
          run:
            path: /bin/bash
            args:
              - -c
              - |
                set -e
                item="$(cat queue/item)"
                [[ ! "$item" ]] && exit 1
                collection_id=`echo $item | jq -r '.collection_id'`
                slug=`echo $item | jq -r '.slug'`
                name=`echo $item | jq -r '.name'`
                repo_name=`echo $item | jq -r '.repository_name'`
                ROOT_DIR=`pwd`
                mkdir -p tmp
                mkdir -p /root/.ssh
                cd tmp
                echo "$slug $collection_id" > ./archive-syncfile
                CODE_DIR=$ROOT_DIR/content-synchronizer/resource-synchronizer SERVER=cnx.org ../content-synchronizer/resource-synchronizer/sync.sh -u ((github:username)) -p ((github:token)) -e ((github:email)) -r $repo_name -q ((github:organization)) -c
                exit 0
